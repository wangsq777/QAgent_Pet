# Render Blueprint
# 部署命令: render blueprint create

services:
  - type: web
    name: qagent-pet
    runtime: python
    region: oregon  # 可选: oregon, frankfurt, singapore, ohio
    plan: free  # 可选: free, starter, pro
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: LLM_API_KEY
        sync: false  # 需要在 Render Dashboard 中手动设置
      - key: LLM_BASE_URL
        value: "https://api.minimaxi.com/anthropic"
      - key: LLM_MODEL
        value: "MiniMax-M2.5"
      - key: WEATHER_API_KEY
        sync: false
      - key: DATABASE_URL
        value: "sqlite+aiosqlite:///./qagent_pet.db"
      - key: PORT
        value: "10000"
      - key: CORS_ORIGINS
        value: "https://your-frontend.onrender.com"  # 生产环境替换为实际前端域名
      - key: API_KEY
        sync: false  # 生产环境必须在 Render Dashboard 中设置为强密码
